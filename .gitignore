# Local secrets
.env

# Nothing here is a bind mount, but ignore stray volume dumps if you create any.
cs2-data/
mariadb-data/
*.log
instances/
.claude/
.codex/

# Built plugin artifacts (regenerated by plugin/build.sh).
plugin/build/
plugin/**/obj/
plugin/**/bin/

# Downloaded mod bundle (regenerated by mods/build.sh).
mods/bundle/

# Assembled shared base install (regenerated by the orchestrator on startup).
base/

# Download cache (assets) + autocert TLS cache, both under the repo now.
cache/

# Build/release outputs.
csfleet
frontend/build/
dist/

# claude
docs/
