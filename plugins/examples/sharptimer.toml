[source]
type  = "github_release"
repo  = "Letaryat/poor-sharptimer"
asset = '^SharpTimer-.*\.zip$'

[[layout]]
from = "SharpTimer-*" # the wrapper dir; the glob tolerates the version stamp
to = "." # it contains addons/ + cfg/, so straight into csgo/
