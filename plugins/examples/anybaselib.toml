# AnyBaseLib — shared base library used by WeaponPaints.
# Shape A: archive is already game-relative
# (addons/counterstrikesharp/shared/AnyBaseLib/...), so the default layout applies.
name = "AnyBaseLib"

[source]
type  = "github_release"
repo  = "NickFox007/AnyBaseLibCS2"
asset = '^AnyBaseLib\.zip$'
