# PlayerSettings — per-player settings store used by WeaponPaints.
# Shape A: ships addons/counterstrikesharp/{plugins/PlayerSettings,shared/PlayerSettingsApi}.
name = "PlayerSettings"

[source]
type  = "github_release"
repo  = "NickFox007/PlayerSettingsCS2"
asset = '^PlayerSettings\.zip$'
